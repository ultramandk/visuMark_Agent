\documentclass{SYSUReport}
\usepackage{tabularx} % 在导言区添加此行
\usepackage{array}
\usepackage{float}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{svg}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{fontspec}

\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shadows.blur, backgrounds}


% --- 定义颜色 (模仿 VS Code 或常见 IDE) ---
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}
\definecolor{codeblue}{rgb}{0.1,0.1,0.9}

% --- 通用设置 ---
\lstset{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\small,          % 使用等宽字体
    breakatwhitespace=false,         
    breaklines=true,                 % 自动折行
    captionpos=b,                    % 标题在下
    keepspaces=true,                 
    numbers=none,                    % 行号在左
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=4,
    frame=single,                    % 代码框
    rulecolor=\color{black}          % 边框颜色
}

% --- Python 特定样式 ---
\lstdefinestyle{pythonstyle}{
    language=Python,
    morekeywords={self, cls, @property}, % 可以添加额外的关键字
    deletekeywords={list,dict}           % 如果不想高亮某些词可以删除
}

% --- C 特定样式 ---
\lstdefinestyle{cstyle}{
    language=C,
    morekeywords={printf, scanf, uint32_t, uint16_t, size_t}, % 添加 C 常用函数或类型
}
% --- 终端/Shell颜色定义 ---
\definecolor{termbackslash}{rgb}{0.2,0.2,0.2} % 深灰色背景
\definecolor{termwhite}{rgb}{0.94,0.94,0.94}  % 接近白色的文字

% --- Terminal 特定样式 ---
\lstdefinestyle{termstyle}{
    language=bash,
    backgroundcolor=\color{termbackslash}, % 使用深色背景模拟终端
    basicstyle=\ttfamily\small\color{termwhite}, % 白色文字
    keywordstyle=\color{cyan}\bfseries,       % 指令用青色高亮
    commentstyle=\color{gray},
    stringstyle=\color{codepurple},
    numbers=none,                             % 终端通常不需要行号
    showstringspaces=false,
    frame=single,                             % 单线框
    rulecolor=\color{gray},
    breaklines=true,
    % 定义常用的 Linux/开发指令
    morekeywords={gcc, g++, python3, make, cmake, ./, mpirun, mpicc, ls, cd, sudo, pip},
    % 自动高亮提示符 $ 符号
    literate={\$}{{\textcolor{green}{\$}}}1,
}
\headr{多模态大模型原理与应用}
\lessonTitle{多模态大模型原理与应用课程报告}
\reportTitle{基于视觉理解与自动操作的智能浏览器助手}
\stuname{}
\stuid{}
\inst{计算机学院}
\major{计算机科学与技术}
\date{}
\lstset{ 
    language=SQL,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{gray},
    stringstyle=\color{red},
    showstringspaces=false,
    breaklines=true,
    numbers=none,
    frame=single
}
\lstdefinelanguage{SQL}{
  morekeywords={SELECT, FROM, WHERE, ORDER, BY, GROUP, HAVING, INSERT, INTO, VALUES,
                UPDATE, SET, DELETE, CREATE, TABLE, DROP, ALTER, AS, JOIN, LEFT,
                RIGHT, INNER, OUTER, ON, LIMIT, OFFSET, DISTINCT, COUNT, SUM,
                AVG, MAX, MIN, LIKE, IN, NOT, NULL, IS, EXISTS, CASE, WHEN, THEN,
                ELSE, END, AND, OR, BETWEEN},
  sensitive=false,
  alsoletter={_},
  morecomment=[l]{--},
  morecomment=[s]{/*}{*/},
  morestring=[d]',
  morestring=[d]"
}
\lstdefinelanguage{Python}{
  morekeywords={%
    and, as, assert, async, await, break, class, continue, def, del, elif,
    else, except, False, finally, for, from, global, if, import, in, is,
    lambda, None, nonlocal, not, or, pass, raise, return, True, try, while,
    with, yield, self, print, len, range, enumerate, zip, map, filter,
    isinstance, type, str, int, float, list, dict, set, tuple, open, close
  },
  sensitive=true,
  morecomment=[l]{\#},
  morestring=[b]",
  morestring=[b]',
  morestring=[s]{"""}{"""},   % 支持三引号字符串
  morestring=[s]{'''}{'''}
}
\lstdefinelanguage{C++}{
  morekeywords={%
    alignas, alignof, and, and_eq, asm, auto, bitand, bitor, bool, break,
    case, catch, char, char8_t, char16_t, char32_t, class, compl, const,
    consteval, constexpr, const_cast, continue, co_await, co_return, co_yield,
    decltype, default, delete, do, double, dynamic_cast, else, enum, explicit,
    export, extern, false, float, for, friend, goto, if, inline, int, long,
    mutable, namespace, new, noexcept, not, not_eq, nullptr, operator, or,
    or_eq, private, protected, public, register, reinterpret_cast, requires,
    return, short, signed, sizeof, static, static_assert, static_cast, struct,
    switch, template, this, thread_local, throw, true, try, typedef, typeid,
    typename, union, unsigned, using, virtual, void, volatile, wchar_t,
    while, xor, xor_eq,
    % 常用标准库函数/类型（可选）
    cout, cin, endl, string, vector, map, set, pair, make_pair, size_t,
    size, push_back, emplace_back, begin, end, find, sort, swap, move
  },
  sensitive=true,
  alsoletter={_},
  morecomment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]",
  morestring=[b]',
  morestring=[s]{R"xxx(}{)xxx"} % 支持原始字符串字面量（简化处理，实际需更复杂规则）
}
\lstdefinelanguage{Java}{
  morekeywords={%
    abstract, assert, boolean, break, byte, case, catch, char, class,
    const, continue, default, do, double, else, enum, extends, final,
    finally, float, for, goto, if, implements, import, instanceof, int,
    interface, long, native, new, package, private, protected, public,
    return, short, static, strictfp, super, switch, synchronized, this,
    throw, throws, transient, try, void, volatile, while, false, null, true
  },
  sensitive=true,
  morecomment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]",
  morestring=[b]'
}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\begin{document} %在document环境中撰写文档

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% 封面及目录，无需改动此处代码
% 面页，需要在导言区用\sysucseset命令填写基本信息
\cover
% 目录
%\tableofcontents
\cleardoublepage
% 设置正文页眉页脚格式，并重新设置页码为1
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\fontsize{12}{18}\selectfont
\tableofcontents

\section{应用背景}
随着互联网的快速发展，网页的样式和信息量都在经历日新月异地增长，为人们完成特定的网页任务带来了挑战。
在多模态大模型技术成熟的背景下，基于VLM/LLM技术实现的多模态网页Agent能够实现辅助和某种程度地替代人类
完成对应的网页任务，具有广阔的应用前景。Web Agent可以作为自动化测试工具，辅助软件开发团队进行前端的
冒烟测试与回归测试。Web Agent也可以作为智能个人助理，帮助个人用户执行复杂任务，降低网页任务的操作成本。
除此以外，Web Agent可以实现复杂数据的收集与分析、改进针对障碍人士的辅助功能等任务。

\section{任务目标}
我们计划实现一个能够理解自然语言指令，并通过浏览器自主完成复杂任务的智能代理。

用户将网页任务目标作为输入，智能代理将通过浏览器自主完成任务，并在前端界面实时展示操作过程。为实现这一目标，我们将设计一个基于视觉理解的网页Agent系统，结合VLM/LLM技术，实现对网页的感知、推理、执行和验证。

\section{系统方案设计}

\subsection{SoM视觉输入模式}

SoM（Set-of-Mark）模式\cite{som}是系统的主要运行模式。该模式将网页截图与可交互元素的视觉标记相结合\cite{webgum}，使VLM能够直观地看到页面上的可操作元素，并通过引用标记编号来精确指定操作目标。其整体架构如图\ref{fig:arch_som}所示，遵循ReAct范式，每步操作分为感知、推理、执行和验证四个阶段\cite{webagent_survey,openclaw}。

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.6\textwidth]{arch_som.png}
    \caption{SoM视觉输入模式的系统架构}
    \label{fig:arch_som}
\end{figure}

\subsubsection{感知阶段}

感知阶段负责将当前网页的视觉状态转化为VLM可以理解的结构化表示。该阶段的输入为Playwright控制的浏览器页面\cite{playwright}，输出为SoM标注后的截图\cite{rvlm}以及可交互元素列表。

\paragraph{可交互元素提取}

\texttt{ElementExtractor}模块通过CSS选择器扫描页面DOM，提取所有可交互元素。主选择器覆盖了HTML原生交互标签如\texttt{<button>}、\texttt{<a>}、\texttt{<input>}、\texttt{<select>}、\texttt{<textarea>}、WAI-ARIA角色属性、事件处理器标记、无障碍标签等属性\cite{webvoyager}。

\begin{lstlisting}[language=Python, caption=INTERACTIVE\_SELECTOR定义, label=lst:selector]
INTERACTIVE_SELECTOR = (
    "button, a, input, select, textarea, "
    "[role='button'], [role='link'], [role='checkbox'], "
    "[role='radio'], [role='combobox'], [role='listbox'], "
    "[role='menu'], [role='tab'], [role='switch'], "
    "[onclick], [tabindex], "
    "[aria-label], [data-a11y]"
)
\end{lstlisting}

对于采用React、Vue等前端框架构建的SPA页面，按钮元素通常为纯\texttt{<div>}或\texttt{<span>}标签，不含任何上述可匹配属性。此时提取模块会遍历页面所有元素，检测其计算样式中\texttt{cursor}属性是否为\texttt{pointer}，并结合可见文本长度识别为框架按钮。这一策略能够有效捕获纯div按钮。

所有候选元素经过以下过滤后，按照视觉位置排序并分配编号：
\begin{itemize}
    \item \textbf{可见性过滤}：排除\texttt{display:none}、\texttt{visibility:hidden}、\texttt{opacity:0}的元素，这些元素渲染后在视觉上不可见。
    \item \textbf{尺寸过滤}：排除面积小于4px²的微小元素。
    \item \textbf{交互性过滤}：排除\texttt{disabled}属性和\texttt{pointer-events:none}样式的不可交互元素。
\end{itemize}

滤除这些明显不可操作的元素，能够大大减少som标注图片中的视觉干扰，提升VLM的定位准确率。

\paragraph{视觉标记绘制}

\texttt{SoMMarker}模块使用PIL库在页面截图上绘制交互元素的视觉标记。为每个候选元素绘制指定颜色的矩形边框，并在边框上方或内侧放置带纯色背景的数字标签。标签采用12种高对比度颜色循环分配，相邻元素尽量使用不同颜色以便于区分。当标签因元素密集而可能重叠时，模块自动将标签移至下一个非重叠候选位置，确保所有标签清晰可读。

例如，bing主页的SoM标记图如图\ref{fig:som_eg}所示：

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{som_eg.png}
    \caption{SoM标记图示例}
    \label{fig:som_eg}
\end{figure}

\paragraph{DOMBridge映射}

\texttt{DOMBridge}维护SoM标记编号与DOM元素之间的双向映射关系。每个标记编号被映射至三个关键信息：元素的CSS选择器、元素的边界框以及后端节点ID。在执行阶段，通过\texttt{get\_bbox(id)}可获取指定编号元素的边界框，\texttt{som\_id\_to\_selector(id)}可将编号转换为Playwright定位器，从而能够执行点击或输入操作。

\subsubsection{推理阶段}

推理阶段将感知阶段的结构化输出组装为VLM可理解的prompt，调用模型获取下一步操作决策\cite{fara7b}。

\paragraph{Prompt组装}

\texttt{build\_som\_user\_prompt}函数负责组装user prompt，其结构包含以下部分：
\begin{itemize}
    \item \textbf{用户任务描述}：用户的自然语言任务。
    \item \textbf{当前页面信息}：页面URL与标题。
    \item \textbf{当前计划}：从上一步推理输出中提取，帮助模型保持任务连贯性。
    \item \textbf{历史操作记录}：最近5步，以\texttt{[OK]}/\texttt{[NO EFFECT]}/\texttt{[FAIL]}前缀区分执行结果。
    \item \textbf{可交互元素文字列表}：每行格式为\texttt{[编号] <标签> "文本内容" aria="无障碍标签"}，供模型与截图中的视觉标记交叉参照。
\end{itemize}
完整的prompt由上述user prompt与system prompt拼接而成。system prompt包含角色设定、响应格式、可用动作表、规则，辅助VLM理解并规划任务步骤。

\paragraph{登录与验证码的人机协同机制}

现今大量网页需要进行登录或验证才能体验完整功能，而agent本身难以独立完成验证码识别和登录流程，因此一定场景下，人工的介入是有必要的。

系统采用"自动检测 + VLM视觉判断"双层策略应对登录和验证码场景。程序化检测层在感知阶段运行，通过扫描页面DOM中的密码输入框、扫码登录关键词，快速识别登录页面。检测到后，Agent直接暂停并通知前端等待人工介入。

VLM视觉判断层作为补充，当VLM在截图或元素列表中识别到验证码、登录表单或扫码界面时，输出\texttt{captcha}或\texttt{login}操作。这两种操作被定义为终端操作，执行后跳过验证阶段，直接进入暂停等待。用户完成登录或验证，点击继续后Agent重新感知页面并恢复任务循环。

\subsubsection{执行阶段}

执行阶段将VLM的决策转化为浏览器实际操作，并处理操作后的页面状态变化\cite{smartllm}。

\paragraph{多层回退点击}

\texttt{ActionExecutor}负责执行VLM输出的操作指令。

例如对于CLICK操作，采用3层回退机制以确保最大的执行可靠性：

\begin{enumerate}
    \item \textbf{Playwright点击}：产生浏览器原生事件，适用于大多数标准元素。
    \item \textbf{JS MouseEvent派发}：通过\texttt{dispatchEvent}派发完整的mousedown/mouseup/click事件序列。当Playwright点击无效时，此方法可绕过部分元素的事件拦截。
    \item \textbf{坐标回退}：通过获取元素坐标，执行Playwright屏幕坐标点击。
\end{enumerate}

对于TYPE操作，执行器强制要求先通过JS \texttt{focus()}和\texttt{select()}将焦点赋予目标输入框，清除已有内容后再通过Playwright键盘输入新文本。

\paragraph{新标签页检测与同步}

执行CLICK或PRESS操作后，系统检测浏览器上下文中的页面数量变化。若检测到新打开的标签页，自动将当前操作页面切换至最新标签页，同时更新截图和前端显示，确保Agent始终在最新的页面上下文中进行下一步操作。

\subsubsection{验证阶段}

验证阶段在执行完成后立即对操作效果进行评估，为下一轮推理提供反馈信息。

\paragraph{全图像素对比}

通过PIL的\texttt{ImageChops.difference}对操作前后的页面截图进行逐像素相减，统计差异像素比例。若差异像素少于$0.05\%$，直接判定操作无效，跳过后续验证步骤。

\paragraph{DOM错误检测}

在像素对比之后通过JavaScript扫描页面DOM中的错误提示元素。检测目标包括WAI-ARIA标准角色、通用错误toast和表单验证消息。

\paragraph{验证反馈与历史记录}

验证结果包括观察描述、是否应当重试、回退操作、替代操作。验证结果被记录在\texttt{StepRecord.verification}字段中。下一轮推理时\texttt{build\_som\_user\_prompt}将最近步骤的验证结果格式化为\texttt{[OK]}（验证通过）、\texttt{[NO EFFECT]}（操作无效）或\texttt{[FAIL]}（执行失败）标记，连同失败原因一起展示在历史记录中。当连续出现2次以上失败标记时，prompt中额外添加警告信息，引导VLM尝试完全不同的操作策略。

\subsubsection{前端Web交互界面}

前端基于FastAPI WebSocket与纯HTML/CSS/JavaScript构建，核心功能包括任务配置、Agent步骤实时展示、浏览器画面实时流和键鼠交互反馈。

\paragraph{实时截图流}

前端通过独立WebSocket通道接收浏览器实时画面流。后端利用Playwright的CDP接口启动\texttt{Page.startScreencast}，以960×540分辨率、约15fps的频率持续截取当前标签页画面，通过专用WebSocket推送至前端。该通道与控制消息WebSocket完全分离，避免了高频帧数据阻塞控制消息的传输。

Screencast模块在浏览器切换标签页时自动检测并重建CDP会话，当帧超时或会话断开时，通过\texttt{env.get\_cdp\_session()}重新获取新标签页的CDP连接并重启推流，确保实时画面始终跟随Agent当前操作的标签页。

\paragraph{键鼠交互反馈}

前端的实时显示窗口可以直接接收用户的键盘和鼠标事件如click、wheel、keydown等，将事件类型与坐标发送至后端。后端通过CDP的键鼠操作等接口将用户操作转发至浏览器，实现用户在Web界面上直接操控Agent浏览器窗口的能力。


\subsection{HTML文本输入模式}

SoM模式通过标注工具标记元素后将截图传给VLM，模型根据视觉信息选择元素以及对应的操作，且在每一步操作都需重新提交网页截图，具有响应时间长、吞吐量低与推理成本高等问题。为此设计了HTML文本输入模式。将页面的精简HTML文本作为输入\cite{weblinx}，滤过
不可见与不可交互元素后，将剩余元素传入预训练的语言模型并输出排序后的元素结果\cite{prune4web,embedrank}，截断排名靠后的元素并将剩余元素列表交给LLM选择。HTML输入模式的后续执行与验证循环和SoM模式共用一套框架。
SoM模式与HTML模式的架构对比
如图\ref{fig:html_mode}所示：

\begin{figure}[H]
\centering
\includegraphics[width=0.6\textwidth]{html_mode.png}
\caption{SoM模式与HTML模式的架构对比}
\label{fig:html_mode}
\end{figure}

\section{实验评测与分析}
针对项目模型的SoM输入与HTML输入模式分别设计了评测实验。所有本地模型均在一台RTX 3090 24GB $\times 1$，内存60GB的计算机上运行，并使用Qwen 3VL-8B-Instruct作为本地测试模型,使用API调用Qwen3.7-max测试。
选取任务成功率(SR)、元素准确率(Ele. Acc)、任务单步操作的F1分数(OP.F1)、任务单步成功率(Step SR)\cite{mind2web}以及单步平均耗时作为评价Web Agent性能的标准。
其中，Ele. Acc表示各任务内选择正确网页元素的步数比例跨任务的均值，Op.F1分数表示单步操作的F1值，Step SR表示各任务内元素与操作均正确的步数比例跨任务的均值，SR表示所有步骤均成功执行的任务数占数据集总任务数的比例。

\begin{align*}
    \text{Ele.Acc} &= \frac{1}{M} \sum_{i=1}^{M} \frac{1}{N_i} \sum_{j=1}^{N_i} \mathbb{I}(\hat{e}_{i,j} \in S_{acc}(e_{i,j})) \\[6pt]
    \text{Op.F1}_{i,j} &= \mathbb{I}\bigl(\hat{op}_{i,j} \in S_{op}(op_{i,j})\bigr) \times
                         \begin{cases}
                            1 & op_{i,j} = \text{CLICK} \\[4pt]
                            \text{F1}(v_{i,j}, \hat{v}_{i,j}) & op_{i,j} \in \{\text{TYPE}, \text{SELECT}\}
                         \end{cases} \\[4pt]
    \text{Step SR} &= \frac{1}{M} \sum_{i=1}^{M} \frac{1}{N_i} \sum_{j=1}^{N_i} \Bigl[ \mathbb{I}(\hat{e}_{i,j} \in S_{acc}(e_{i,j})) \times \mathbb{I}(\text{Op.F1}_{i,j} = 1) \Bigr] \\[6pt]
    \text{SR}_i &= \prod_{j=1}^{N_i} \Bigl[ \mathbb{I}(\hat{e}_{i,j} \in S_{acc}(e_{i,j})) \times \mathbb{I}(\text{Op.F1}_{i,j} = 1) \Bigr]
\end{align*}
其中$M$为数据集内的总任务数，$N_{i}$为第$i$个任务的步骤数，$e_{i,j},\hat{e}_{i,j}$分别为第$i$个任务第$j$步中Ground Truth的等价网页元素集合和Agent预测的网页元素，$op_{i,j} \in \{\text{CLICK},\text{TYPE},\text{SELECT}\}$为Ground Truth操作类型，$\hat{op}_{i,j}$为预测操作类型，$v_{i,j},\hat{v}_{i,j}$分别为Ground Truth和预测的操作值。
$S_{op}(\text{CLICK})=\{\text{CLICK}, \text{HOVER}, \text{PRESS}\}$为Mind2Web中将$\text{HOVER}$和$\text{PRESS\_ENTER}$映射至$\text{CLICK}$的等价操作集合；$\text{F1}(\cdot,\cdot)$为F1分数。

\subsection{HTML输入模式下的性能测评}
选用Mind2Web数据集对模型进行评测。Mind2Web数据集是一个大规模的Web Agent基准数据集,将真实世界的静态网页作为数据,具有1,341个不同任务和46步的最长任务步数，在Web Agent领域被广泛用于测试模型的页面理解与执行能力。

我们对Mind2Web数据集提供的评测方法进行了修改。在Mind2Web数据集提供的HTML应用过滤规则只保留可交互元素后，使用一个基于Bert2实现的预训练模型all-MiniLM-L6-v2对剩余候选元素进行排序并交给VLM进行选择，并给出对应的操作类型和元素，最后计算Ele. Acc,OP.F1,Step SR和SR。
\begin{table}[H]
\centering
\small
\caption{HTML输入模式下模型在Mind2Web数据集上的性能}
\label{tab:html_perf}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{l|rrrr|rrrr|rrrr|r}
\toprule
& \multicolumn{4}{c}{Cross-Task} & \multicolumn{4}{c}{Cross-Website} & \multicolumn{4}{c}{Cross-Domain} & \\
\cmidrule(lr){2-5} \cmidrule(lr){6-9} \cmidrule(lr){10-13}
& EA & OP & SS & SR & EA & OP & SS & SR & EA & OP & SS & SR & Time(s) \\
\midrule
Qwen3-VL-8B & 41.9 & 84.0 & 33.9 & 1.5 & 32.0 & 78.0 & 26.4 & 1.0 & 36.0 & 83.0 & 28.0 & 3.0 & 1.3 \\
Qwen3.7-max & 64.6 & 80.5 & 52.0 & 8.7 & 57.9 & 81.3 & 42.4 & 6.2 & 55.6 & 81.9 & 43.8 & 7.3 & 14.3 \\
\bottomrule
\end{tabular}
\end{table}
由表1可以看出，Qwen3.7-max在元素定位上显著优于Qwen3-VL-8B，元素准确率平均比Qwen3-VL-8B提高了$22.7\%$，最终导致更高的单步成功率与任务成功率。但Qwen3-VL-8B作为小模型具有快速响应与低成本
的优势，在相对较好的网页操作性能和较快的响应时间中取得了平衡。Qwen3.7-max的参数量过大，虽然准确率更高但响应时间是Qwen3-VL-8B的11倍，不具有实时性。
\subsection{SoM输入模式下的性能测评}

为评估SoM视觉输入模式在真实网页场景下的表现，自行设计了包含30个任务的测试集，全部基于可公开访问的真实网站。测试任务覆盖多种难度与应用场景，代表性任务包括：搜索"中山大学广州校区南校园"并返回地址、打开哔哩哔哩首页点击第一个视频后返回作者头像、登录QQ邮箱并向指定地址发送端午祝福邮件等。所有测试均使用Qwen 3VL-8B-Instruct模型，在无头浏览器模式下运行，每个任务至少重复运行10次以减小随机性。

由于自建测试集缺少Mind2Web数据集中的Ground Truth标注（如真实操作序列和正负候选元素），无法直接计算Ele. Acc和Op.F1等微观指标。因此针对真实网页场景，定义以下两项宏观指标评价Agent的任务完成能力：

\begin{align*}
    \text{SR} &= \frac{S}{N} \\
    \text{AvgSteps} &= \frac{1}{S} \sum_{i \in \mathcal{S}} \text{steps}_i
\end{align*}
其中$N=30$为测试任务总数，$S$为成功完成（Agent返回正确答案且人工验证通过）的任务数，$\mathcal{S}$为成功任务集合，$\text{steps}_i$为第$i$个任务成功完成时使用的步数。

\begin{table}[H]
\centering
\small
\caption{SoM输入模式与HTML输入模式在自建测试集上的对比}
\label{tab:som_vs_html}
\setlength{\tabcolsep}{6pt}
\begin{tabular}{l|rr|rr}
\toprule
\multirow{2}{*}{模型} & \multicolumn{2}{c|}{SoM输入} & \multicolumn{2}{c}{HTML输入} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
& SR & AvgSteps & SR & AvgSteps \\
\midrule
Qwen 3VL-8B & 73.3 & 10.5 & 40.0 & 6.8 \\
\bottomrule
\end{tabular}
\end{table}

由表2可以看出，SoM视觉输入模式相比HTML文本输入模式在真实网页任务上具有显著优势。任务成功率从$40.0\%$提升至$73.3\%$，提升幅度达$83.3\%$。分析任务完成情况发现，HTML输入模式能够成功完成的任务以简单的搜索引擎查询为主（如事实类搜索、天气查询等），一旦任务涉及表单填写、页面跳转、多步交互等复杂操作，成功率显著下降。SoM模式由于能够直观地识别页面布局与元素空间位置，在需要点击特定按钮、填写表单、切换标签页等复杂操作时表现更好。但SoM模式的完成任务平均步数$10.5$步明显高于HTML模式的$6.8$步，这一方面是因为SoM模式成功完成的任务本身更复杂，自然需要更多操作步数；另一方面也与SoM模式下模型倾向于执行更多探索性操作（如下拉滚动、尝试不同元素）有关。

总体而言，SoM输入模式以更多的操作步数为代价，换取了更高的任务完成率与更广泛的任务适用性，在真实网页场景下优于HTML文本输入模式。


\section{错误分析与AGI局限性讨论}

前文的实验评测揭示了当前VLM驱动的Web Agent在真实网页场景中的多项性能瓶颈。本节通过对典型错误案例的深入分析，建立系统性的错误分类体系，并在此基础上讨论当前AGI技术在Web Agent领域的能力边界与根本性局限。

\subsection{典型错误案例分析}

为深入理解Agent的失败模式，我们从Mind2Web数据集评测和自建SoM测试集中选取了具有代表性的错误案例进行分析。每个案例展示了Agent在某类典型场景下的失败过程与根因。

\subsubsection{案例一：密集布局下的元素选择错误}

\textbf{任务场景}：在B站首页的分区选择页面中，Agent需要点击"影视"分区按钮进入对应分区。该页面分类标签密集排列，且页面中央同时弹出了活动推广弹窗，弹窗的半透明边框与分区按钮的SoM标记在视觉上高度重叠。

\textbf{Agent行为}：VLM在SoM标注截图中将第\#91号元素（影视分区按钮）与第\#57号元素（弹窗的半透明边框区域）混淆，错误地输出了 \texttt{CLICK \#57}。点击弹窗边框后页面未跳转至影视分区，Agent继续在错误页面上执行后续操作，最终任务失败。

\textbf{根因分析}：在密集、重复的UI布局中，VLM无法准确区分相邻编号。此外，大部分元素不含有效的语义标签，无法提供有效的语义区分，VLM仅凭视觉位置信息难以做出精确判断，最终选择错误元素。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/som_march_error.png}
    \caption{密集布局下的元素选择错误示例}
    \label{fig:error_dense}
\end{figure}

\subsubsection{案例二：SoM标签注入错误导致的操作无效}

\textbf{任务场景}：在某饰品交易网站首页，Agent需要点击"饰品市场"入口按钮进入商品列表页。SoM标注将该按钮标为\#2号元素。

\textbf{Agent行为}：VLM正确识别了目标按钮（输出 \texttt{CLICK \#2}），但进行操作后页面没有正确跳转，Agent在下一步中重复尝试同一元素，陷入循环直至超时。

\textbf{根因分析}：该案例暴露了两层问题。第一层是DOM结构层面：现代前端框架构建的按钮具有极深的嵌套结构——例如一个按钮可能由外部容器、内部图标层、\texttt{<svg>}矢量图形和文本标签等多层嵌套组成，且每一层均无\texttt{id}、\texttt{role}或\texttt{aria-label}等可用于精确定位的语义标记。SoM提取器在扫描DOM时将外层容器识别为可交互元素，但在注入\texttt{data-som-id}时，CSS选择器可能命中内部的图标或文本节点，导致标记被放置在非交互子元素上。后续Playwright根据标记编号查找元素时，找到的是没有事件处理的子节点，点击无法触发父层的事件处理逻辑。第二层是Agent推理层面：Agent缺乏对"点击后页面应如何变化"的预期模型，因此在操作无响应时无法判断是"元素不对""方式不对"还是"页面卡顿需等待"，只能盲目重试。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/search_error.png}
    \caption{CLICK操作执行失败示例}
    \label{fig:error_exec_fail}
\end{figure}

\subsubsection{案例三：页面状态已达目标但Agent继续操作}

\textbf{任务场景}：在比较茅台与腾讯股价涨幅的任务中，Agent已成功打开茅台股价信息页面，但未能识别到当前页面即为目标页面，继续执行多余的搜索操作。

\textbf{Agent行为}：VLM未能识别到答案已在当前页面可见（搜索框下方已展示茅台股价卡片），而是继续滚动页面、点击无关元素，直至消耗全部30步配额后以失败告终。

\textbf{根因分析}：该案例暴露了VLM在"答案识别"与"任务终止判断"上的核心弱点。尽管System Prompt明确要求"一旦看到答案立即输出answer"，VLM在实际推理中容易陷入"继续探索"的行为模式，尤其在于页面信息密度高、目标答案在视觉上不够突出时。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/error_no_effect.png}
    \caption{操作无效错误示例}
    \label{fig:error_no_effect}
\end{figure}

\subsection{错误分类体系}

基于上述案例分析和评测数据，我们将Web Agent的失败模式归纳为三个层次、七个类别的系统化错误分类体系。该分类覆盖了从感知输入到任务完成的完整链路。

\begin{table}[H]
\centering
\small
\caption{Web Agent错误分类体系}
\label{tab:error_taxonomy}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{c|p{5cm}|p{4.5cm}|c}
\toprule
\textbf{层次} & \textbf{错误类别} & \textbf{典型表现} & \textbf{估计占比} \\
\midrule
\multirow{3}{*}{感知层}
    & 1. 元素漏检 & 可交互元素未被CSS选择器或其他策略覆盖 & 极少 \\
    & 2. SoM标记冲突 & 密集布局中边界框重叠，标签相互遮挡 & 极少 \\
    & 3. SoM注入有误 & dom嵌套结构复杂，som-id没有正确注入到可交互元素上 & 较多 \\
\midrule
\multirow{3}{*}{推理层}
    & 4. 操作选择错误 & VLM选错元素编号或操作类型 & 最多 \\
    & 5. 任务步骤规划错误 & 模型规划的步骤与实际需求不符 & 较多 \\
    & 6. 任务终止误判 & 过早Answer（信息不完整）或过晚Answer（额外探索） & 较少 \\
\midrule
\multirow{1}{*}{执行层}
    & 7. 点击/输入执行失败 & 元素不可见、浏览器拦截 & 极少 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    % 预留：错误分布饼图/柱状图
    \includegraphics[width=0.8\textwidth]{figures/error_distribution.png}
    \caption{错误类别分布统计}
    \label{fig:error_dist}
\end{figure}

\subsection{AGI局限性讨论}

上述错误分析不仅揭示了当前Web Agent系统的工程改进方向，更指向了当前通用人工智能技术在真实场景中面临的若干根本性局限。

\subsubsection{视觉理解 $\neq$ 语义理解}

当前VLM对网页截图的理解本质上是模式匹配而非语义理解。模型通过大规模预训练学习了像素模式与文本之间的统计关联，但缺乏对UI元素的"功能性理解"。具体表现为：模型可以在一张包含搜索按钮的截图中识别出"这是一个搜索按钮"，但该识别依赖于视觉特征（按钮形状、颜色、文字标签）与训练数据中相似模式的匹配。当面对全新的、与训练分布差异较大的UI设计时（如非标准控件、定制化组件），模型的识别准确率显著下降。这种"见多识广但未真正理解"的特性是当前视觉语言模型的根本局限。

证据来自实验数据：SoM模式下任务成功率$73.3\%$相比HTML模式下$40.0\%$（纯文本，无视觉信息）的$83.3\%$提升表明视觉信息至关重要，但仍有$26.7\%$的任务即使在视觉信息充分的情况下也无法完成，说明"看得到"不等于"看得懂"。

\subsubsection{缺乏真实世界知识与常识推理}

VLM的知识全部来源于训练数据中的文本和图像，缺乏对真实世界的因果理解和常识推理能力。在Web Agent场景中，这表现为：
\begin{itemize}
    \item \textbf{网站行为常识缺乏}：人类知道"点击商品图片通常会进入详情页""购物车图标通常在页面右上角"，这些常识来自大量浏览经验。VLM虽然可能在训练数据中间接学习到部分模式，但缺乏系统性的、可泛化的网站交互常识。
    \item \textbf{任务语义理解浅层化}：当任务要求"找到最便宜的手机并截图"时，人类会自然地比较多个商品的价格数值并选择最小值，而VLM倾向于选择第一个被标记的价格元素即输出answer，缺乏"比较—选择"的深层推理链路。
    \item \textbf{错误恢复缺乏因果推理}：当Agent连续两次操作无效时，VLM被prompt引导"尝试完全不同的策略"，但模型并不真正理解为什么前两次操作失败了，因此"不同的策略"本质上仍是随机的探索。
\end{itemize}


\subsubsection{多步推理与长期规划能力薄弱}

Web Agent的任务本质上是序列决策问题——Agent需要在部分可观测的环境中执行一系列操作以达到目标状态。实验数据揭示了当前模型在长期规划上的明显不足：
\begin{itemize}
    \item \textbf{规划视野短}：SoM模式下成功任务的平均步数为10.5步，但Agent在每一步的推理均以"当前页面看到什么→下一步做什么"为主，缺乏超过2-3步的前瞻性规划。当任务需要先导航到特定页面、再填写表单、最后提交的多阶段流程时，模型容易在阶段转换处迷失。
    \item \textbf{回溯能力缺失}：当Agent发现自己走错路径（如点击了错误的链接进入了无关页面），虽然prompt中提供了GOTO回退和Escape关闭弹窗的指令，但模型缺乏系统性的"状态评估→识别错误→制定恢复计划→执行恢复"能力，更多依赖prompt中的启发式规则。
    \item \textbf{探索-利用平衡差}：SoM模式平均步数（10.5步）显著高于HTML模式（6.8步），部分原因是模型在视觉模式下倾向于进行大量探索性操作（滚动、悬停、点击不同元素查看反应），缺乏高效的探索策略。
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{figures/agi_planning_gap.png}
    \caption{人类与Agent的多步推理路径对比}
    \label{fig:agi_planning}
\end{figure}

综合来看，当前VLM驱动的Web Agent在特定任务类型（简单搜索、表单填写）和特定网站（常见主流网站）上已展现出实用价值。然而，距离真正意义上的"通用网页智能体"，仍需在多模态理解深度、常识推理、长期规划、持续学习和元认知等维度取得根本性突破。这些维度不仅适用于Web Agent场景，也构成了当前AGI研究的核心挑战。

\section{附录}

\begin{thebibliography}{99}
\bibitem{webvoyager} He, Y., et al. “WebVoyager: Building an End-to-End Web Agent with Large Multimodal Models.” 10.48550/arXiv.2401.13919
\bibitem{fara7b} Microsoft Research. “Fara-7B: A Foundation Model for GUI Automation.” 2025.
\bibitem{webagent_survey} Xinyi Li, et al. “A survey on LLM-based multi-agent systems: workflow, infrastructure, and challenges”  Computer Science.10.1007/s44336-024-00009-2
\bibitem{som} Yang, J., et al. “Set-of-Mark Prompting Unleashes Extraordinary Visual Grounding in GPT-4V.” arXiv preprint arXiv:2310.11441, 2023.
\bibitem{rvlm} Cheng, K., et al. “R-VLM: Region-Aware Vision Language Model for Precise GUI Grounding.” arXiv preprint 10.48550/arXiv.2507.05673.
\bibitem{webgum} Furuta, H., et al. “WebGUM: Multimodal Web Navigation with Instruction-Finetuned Foundation Models.” arXiv.2305.11854v4
\bibitem{playwright} Microsoft. “Playwright: Fast and Reliable Cross-Browser Automation.” \url{https://playwright.dev/}, 2025.
\bibitem{openclaw} OpenClaw Contributors. “OpenClaw: Open-Source Multimodal Web Agent.” GitHub, 2025.
\bibitem{smartllm} SMART-Lab Purdue. “SMART-LLM: Multi-Agent Task Planning.” GitHub, 2024.
\bibitem{mind2web} X. Deng, Y. Gu, B. Zheng, S. Chen, S. Stevens, B. Wang, H. Sun, and Y. Su, “Mind2Web: Towards a Generalist Agent for the Web,” in \textit{Advances in Neural Information Processing Systems (NeurIPS)}, vol. 36, pp. 28091–28114, 2023.
\bibitem{weblinx} X. Lù et al., “WebLINX: Real-World Website Navigation with Multi-Turn Dialogue,” in \textit{International Conference on Machine Learning (ICML)}, 2024.
\bibitem{prune4web} J. Li et al., “Prune4Web: DOM Tree Pruning Programming for Web Agent,” arXiv preprint arXiv:2511.21398, 2025.
\bibitem{embedrank} S. Ahmadi et al., “EmbedRank-LM: Natural Language-Driven Web Element Identification for Automated UI Testing,” in \textit{IEEE Access}, 2025.
\end{thebibliography}
\end{document}
